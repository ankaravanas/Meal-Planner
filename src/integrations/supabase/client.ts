/**
 * Supabase Client
 *
 * This demo app uses a mock Supabase client with localStorage.
 * All data is stored locally and persists across browser sessions.
 */

// Use mock client for demo mode
export { supabase } from '@/mocks/mockSupabaseClient';
